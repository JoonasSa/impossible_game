
package fi.joonassa.impossiblegameproject.gui;

import fi.joonassa.impossiblegameproject.actors.Actor;
import fi.joonassa.impossiblegameproject.gamemain.GameMain;
import java.util.ArrayList;

public class PaintComponent {
    
    public void repaint(ArrayList<Actor> objects, Actor Player) {
        /*
        for (int y = 0; y < GameMain.HEIGHT; y++) {
            for (int x = 0; x < GameMain.WIDTH; x++) {
                for (Actor a : objects) {
                    if (a.getY() == y && a.getX() == x) {
                        System.out.print("");
                    }
                }
            }
        }
        */
    }
}
